config:
  target: "http://localhost:8080"
  processor: "./playground.nova-ms.js"
  phases:
    - duration: 20
      arrivalRate: 100
  http:
    timeout: 20
scenarios:
  - name: "Insert / update / delete N items"
    weight: 39
    flow:
      - loop:
          - get:
              url: "/api/scitems"
          - function: "setInsertItemData"
          - post:
              url: "/api/scitems"
              json: "{{ InsertItem }}"
              capture:
                json: "$.Id"
                as: "InsertedItemId"
          - get: 
              url: "/api/scitems/{{ InsertedItemId }}"
              capture: 
                json: "$"
                as: "ViewItem"
          - think: 1
          - function: "setUpdateItemData"
          - put: 
              url: "/api/scitems"
              json: "{{ UpdateItem }}"
          - think: 1
          - get:
              url: "/api/scitems"
          - delete:
              url: "/api/scitems/{{ InsertedItemId }}"
        count: 20
  - name: "View N items"
    weight: 59
    flow:
      - loop:
          - get: 
              url: "/api/scitems"
              capture:
                - json: "$"
                  as: "Items"
          - function: "setRandomItemId"
          - get:
              url: "/api/scitems/{{ RandomItemId }}"
          - think: 1
          - get:
              url: "/api/scitems"
        count: 20
  - name: "Log current number of items"
    weight: 2
    flow:
      - think: 2
      - get:
          url: "/api/scitems"
          capture: 
            json: "$"
            as: "Items"
      - function: "setItemsLength"
      - log: "Total number of item: {{ ItemsLength }}"